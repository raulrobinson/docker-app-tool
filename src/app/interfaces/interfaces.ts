export interface container {
    container_id: string;
    image: number;
    names: string;
    created: string;
    since_time: string;
    ports: string;
    status: string;
    command: string;
    timestamp: string;
}